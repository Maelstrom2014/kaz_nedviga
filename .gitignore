# Python
__pycache__/
*.py[cod]
*.egg-info/

# SQLite (favorites + price history)
data/favorites.db
data/favorites.db-shm
data/favorites.db-wal

# Runtime caches (regenerated by the app)
data/last_results.json
data/rates.json
data/settings.json
data/features.csv

# Photo disk cache
cache/

# Logs (rotating, under logs/)
logs/
*.log

# Telegram bot token (secret — never commit; see telegram_bot.py)
bot_token.txt
.env

# Debug / recon scripts
tools/

# 2gis recon artifacts
data/2gis_recon_*
data/2gis_bundle_*
data/2gis_realty_*
data/phone_recon/

# Design/task docs now live in docs/tasks/ and ARE tracked.
# (old rule `task_*.md` removed 2026-08-31)

# One-off cleanup script
remove_kvartirka.py

# Scratch / cleanup scripts
zz*.py
cleanup_temp.py
_*.py
