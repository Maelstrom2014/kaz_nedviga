[pytest]
# Live-network tests are deselected by default: run them explicitly with
#   pytest -m live
addopts = -m "not live"
markers =
    live: hits real sites over the network (deselected by default)
testpaths = tests
