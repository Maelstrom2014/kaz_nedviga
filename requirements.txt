flask>=3.0.0
requests>=2.31.0
curl_cffi>=0.7.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
fpdf2>=2.7.0
Pillow>=10.0.0
pytest>=8.0.0
# Optional: OLX phone reveal via headless browser (off by default;
# olx_phone_playwright setting). Install browser: playwright install chromium
# playwright>=1.49.0
# Optional: price-quality neural net (ml/train_price_model.py, features_csv.py)
# torch>=2.0.0
